# Subtests

This folder contains tests meant for inclusion in other tests. These are primarily tests that benefit from being tested in multiple different scenarios, and that can be run in a standardized way independently of which site was used for generation. The tests may take additional input to deal with minor differences in the exact input, or for example to identify where to run the tests in the filesystem, but nothing more significant than that.
